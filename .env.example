# YouTube Data API v3 key (https://console.cloud.google.com)
YOUTUBE_API_KEY=

# Reddit API credentials are OPTIONAL. Without them we fall back to the
# public reddit.com/.json endpoint (rate-limited but usable for low volume).
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=social-intel-dashboard/1.0
